---
const title = "Próximamente";
---
<html lang="es">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>{title}</title>
  </head>
  <body>
    <div class="stars"></div>
    <div class="glow-orb glow-orb-1"></div>
    <div class="glow-orb glow-orb-2"></div>

    <main class="center">
      <div class="card promo-card">
        <h1 class="promo-title">Próximamente</h1>
        <p class="promo-text">Aquí habrá más sorpresas. Vuelve pronto ✨</p>
        <a href="/" class="more-btn back-btn">Volver</a>
      </div>
    </main>

    <style>
      :root {
        --primary: #667eea;
        --accent: #f093fb;
        --bg-a: #667eea;
        --bg-b: #764ba2;
      }

      * { box-sizing: border-box; margin:0; padding:0; }

      body {
        font-family: 'Helvetica Neue', Arial, sans-serif;
        min-height: 100vh;
        display: flex;
        align-items: center;
        justify-content: center;
        background: linear-gradient(135deg, var(--bg-a) 0%, var(--bg-b) 100%);
        padding: 24px;
        position: relative;
        color: #fff;
      }

      .stars {
        position: fixed; inset:0; pointer-events:none;
        background-image: 
          radial-gradient(2px 2px at 20% 30%, white, transparent),
          radial-gradient(2px 2px at 60% 70%, white, transparent);
        opacity: 0.6;
      }

      .glow-orb { position: fixed; border-radius:50%; filter:blur(80px); opacity:0.25; pointer-events:none; }
      .glow-orb-1 { width:300px; height:300px; background:#fad0c4; top:8%; right:8%; }
      .glow-orb-2 { width:250px; height:250px; background:#a8edea; bottom:8%; left:8%; }

      .center { width:100%; max-width:720px; padding:20px; }

      .card.promo-card {
        background: rgba(255,255,255,0.96);
        color: #2d3748;
        border-radius: 16px;
        padding: 48px;
        text-align: center;
        box-shadow: 0 30px 80px rgba(0,0,0,0.25);
      }

      .promo-title {
        font-size: 2.4rem;
        margin-bottom: 12px;
        background: linear-gradient(45deg, #fff, #fad0c4);
        -webkit-background-clip: text;
        -webkit-text-fill-color: transparent;
      }

      .promo-text { margin-bottom: 20px; font-size:1.05rem; color: #4a5568; }

      .more-btn, .back-btn {
        display: inline-block;
        padding: 10px 18px;
        border-radius: 10px;
        background: linear-gradient(90deg, var(--primary), var(--accent));
        color: white;
        text-decoration: none;
        font-weight: 600;
        box-shadow: 0 8px 20px rgba(102,126,234,0.18);
        transition: transform .15s ease, box-shadow .15s ease;
      }

      .more-btn:hover { transform: translateY(-3px); box-shadow: 0 12px 30px rgba(102,126,234,0.25); }

      @media (max-width:480px) {
        .card.promo-card { padding: 28px; }
        .promo-title { font-size: 1.6rem; }
      }
    </style>
  </body>
</html>